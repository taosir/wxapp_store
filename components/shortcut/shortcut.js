const App = getApp(); Component({ options: { multipleSlots: true }, properties: { right: { type: String, value: '24rpx' }, bottom: { type: String, value: '250rpx' } }, data: { isShow: false, transparent: true }, methods: { _onToggleShow(e) { App.saveFormId(e.detail.formId); this.setData({ isShow: !this.data.isShow, transparent: false }) }, _onTargetPage(e) { let urls = App.getTabBarLinks(); App.saveFormId(e.detail.formId); wx.switchTab({ url: '/' + urls[e.detail.target.dataset.index] }) } } })